import cactus


def main():
    # One valid 32x32 fully-upgraded chain harvest reaches the complete
    # Leaderboards.Cactus target, so the leaderboard program must only run
    # one finite Cactus cycle and then terminate.
    cactus.run()


main()
